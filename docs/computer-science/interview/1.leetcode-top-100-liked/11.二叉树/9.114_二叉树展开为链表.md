---
title: 114_二叉树展开为链表[MEDIUM]
createTime: 2026/03/22 15:12:32
permalink: /interview/leetcode/114/
---

给你二叉树的根结点 root ，请你将它展开为一个单链表：

展开后的单链表应该同样使用 TreeNode ，其中 right 子指针指向链表中下一个结点，而左子指针始终为 null 。

展开后的单链表应该与二叉树 先序遍历 顺序相同。

 

示例 1：
<img alt="" src="https://assets.leetcode.com/uploads/2021/01/14/flaten.jpg" style="width: 500px; height: 226px;" />

```
输入：root = [1,2,5,3,4,null,6]
输出：[1,null,2,null,3,null,4,null,5,null,6]
```

示例 2：

```
输入：root = []
输出：[]
```

示例 3：

```
输入：root = [0]
输出：[0]
```

## 解题思路

1. 找到左子树的最右节点 （right到底），记为 predecessor 节点
2. 将右子树接到 predecessor 节点的右边 
3. 左子树换到右边，左边置空
4. `curr = curr.right`

## Java实现

```java
	public void flatten(TreeNode root) {
		TreeNode curr = root;
		while (curr != null){
			TreeNode predecessor = findPredecessor(curr.left);
			if (predecessor != null){
				predecessor.right = curr.right;
				curr.right = curr.left;
				curr.left = null;
			}
			curr = curr.right;
		}
	}

	// 寻找  predecessor（右到底） 节点
	private TreeNode findPredecessor(TreeNode root){
		if (root == null){
			return null;
		}
		while (root.right != null){
			root = root.right;
		}
		return root;
	}
```
